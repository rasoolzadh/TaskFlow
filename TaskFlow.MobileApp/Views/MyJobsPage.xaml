<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TaskFlow.MobileApp.ViewModels"
             xmlns:model="clr-namespace:TaskFlow.MobileApp.Models"
             x:Class="TaskFlow.MobileApp.Views.MyJobsPage"
             x:DataType="vm:MyJobsViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto, *">
        <!-- Header and Refresh Button -->
        <HorizontalStackLayout Grid.Row="0" Padding="10" Spacing="10">
            <Label Text="Your Jobs" FontSize="24" FontAttributes="Bold" VerticalOptions="Center"/>
            <Button Text="Refresh" Command="{Binding GetJobsCommand}" IsEnabled="{Binding IsNotBusy}" HorizontalOptions="EndAndExpand"/>
        </HorizontalStackLayout>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <!-- List of Jobs -->
            <CollectionView ItemsSource="{Binding Jobs}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Job">
                        <Frame Padding="15" Margin="10" BorderColor="LightGray" CornerRadius="5">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Client.Name}" FontSize="14" />
                                <Label Text="{Binding Status, StringFormat='Status: {0}'}" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Busy Indicator -->
            <ActivityIndicator IsVisible="{Binding IsBusy}"
                               IsRunning="{Binding IsBusy}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" 
                               Color="Orange" />
        </Grid>
    </Grid>

</ContentPage>
